name: "Unit Tests"
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: generate test coverage
        run: cd server && go test ./... -coverprofile=./cover.out -covermode=atomic -coverpkg=./...
      - name: go-test-coverage
        uses: vladopajic/go-test-coverage@v2.11.2
